'use strict';

module.exports = function (Query) {

  describe('@Query', function () {
    describe('#constructor(rawQuery)', function () {
      it('should only accept an object as parameter', function () {
      });
    });
    describe('#generate(types)', function () {
      it('should call onParseArgs listeners', function () {
      });
      it('should call onGenerateQuery listeners', function () {
      });
      it('should replace all the types in the query by valid GraphQL types', function () {
      });
    });
  });

};
